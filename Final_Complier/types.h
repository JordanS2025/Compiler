#ifndef TYPES_H
#define TYPES_H

// Enumeration for variable types
typedef enum {
    VAR_TYPE_INT,
    VAR_TYPE_FLOAT,
    VAR_TYPE_CHAR,
    VAR_TYPE_UNKNOWN,
    VAR_TYPE_VOID
    // Add other types if needed
} VarType;

#endif // TYPES_H
